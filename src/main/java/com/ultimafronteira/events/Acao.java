package com.ultimafronteira.gamecore;

/**
 * Enum para representar as ações que o jogador pode tomar.
 */
public enum Acao {
    EXPLORAR,
    DESCANSAR,
    AVANCAR_TURNO // Para quando o jogador não faz nada e só passa o tempo
}